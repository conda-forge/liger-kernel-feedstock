schema_version: 1

context:
  version: "0.8.1"

package:
  name: liger-kernel
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/l/liger-kernel/liger_kernel-${{ version }}.tar.gz
  sha256: ae708a8079d2283070128e38497aa2976294d822e21a0f21ea86e63c67334a8b

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=42
    - wheel
    - setuptools-scm
    - pip
  run:
    - python >=${{ python_min }}
    - pytorch >=2.1.2
    - triton >=2.3.1

tests:
  - python:
      imports:
        - liger_kernel
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - '*'

about:
  summary: Efficient Triton kernels for LLM Training
  license: BSD-2-Clause AND MIT AND Apache-2.0
  license_file:
    - LICENSE
    - NOTICE
    - licenses/LICENSE-MIT-Efficient-Cross-Entropy
    - licenses/LICENSE-MIT-triton
    - licenses/LICENSE-MIT-llmc
    - licenses/LICENSE-Apache-2.0
    - licenses/LICENSE-MIT-AutoAWQ
  homepage: https://github.com/linkedin/Liger-Kernel

extra:
  recipe-maintainers:
    - chrisdeutsch
    - LeeNethertonQC
